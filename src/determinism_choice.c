#include <stdio.h>
#include <math.h>
#include "../inc/elements.h"

/**
* Manage the distance between 2 cell.
* assuming we're using the carthesien distance
* between these points.
*
* */
//TODO : buld distance need;
double distance_calculator(cell * c1, cell * c2){
  return sqrt(

  );
}
